const CartIcon = ({ onClick }) => (
  <button onClick={onClick} className="absolute top-4 right-4 text-white bg-[#007580] px-4 py-2 rounded-lg">
    Cart 🛍️
  </button>
);

export default CartIcon;
